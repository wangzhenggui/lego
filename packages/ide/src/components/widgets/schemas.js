import inputSchema from './antd/inputSchema';
import buttonSchema from './antd/buttonSchemas';
import layoutSchema from './antd/layoutSchema';
import layoutHeaderSchema from './antd/layoutHeaderSchema';
import rowSchema from './antd/rowSchema';
import colSchema from './antd/colSchema';
import emptySchema from './antd/emptySchema';
import pageSchema from './pageSchema';

export default { inputSchema, buttonSchema, layoutSchema, layoutHeaderSchema, rowSchema, colSchema, emptySchema, pageSchema };
